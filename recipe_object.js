var recipe = {
	'title': 'Borcsht',
	'servings': 5,
	'ingredients': ['meat', 'water', 'celery', 'beet'],
}

console.log(recipe.title);
console.log(recipe.servings);
for (var i = 0; i < recipe.ingredients.length; i++) {
	console.log(recipe.ingredients[i]);
}