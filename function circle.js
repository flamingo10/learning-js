function calcCircumfrence(r) {
	var pi = Math.PI;
	var l = 2 * pi * r;
		console.log('The circumference is' + l);
}
function calcArea(r) {
	var pi = Math.PI;
	var s = pi * r * r;
		console.log('The area is' + s);
}